# Known differences

There are no approved behavioural differences yet. Proposed entries are design constraints for review, not permission to implement or route traffic.

| ID | Route/journey | Rails behaviour | Proposed or approved replacement | Reason and evidence | Approver | Tests | Status |
|---|---|---|---|---|---|---|---|
| KD-SESSION-001 | Complete OIDC and authenticated boundary, including pre-authentication `gov_one_auth_state`, `gov_one_auth_nonce`, `gov_one_redirect_uri`; logout `id_token`; registration `registration_review`; summative `form_nonce`; and framework identity/CSRF/flash/timeout purposes identified by characterization | Rails/Devise owns authentication and workflow state in the Rails session. Exact cookie attributes, rotation, timeout, logout/replay and GET `/users/sign_out` behaviour remain to be pinned by executed Rails characterization; default CookieStore evidence alone is not treated as proof. | After an atomic authenticated-boundary cutover, the approved ASP.NET Core owner may issue a distinct opaque session representation with separately managed keys. It must own OIDC initiation/callback and all session-coupled routes, reject or ignore Rails cookies, rotate on authentication, enforce the approved timeout, preserve CSRF/workflow-nonce semantics, and implement the approved GOV.UK One Login logout/invalidation behaviour. Rollback may require reauthentication only if explicitly approved and tested without redirect loops. | Rails and ASP.NET Core formats/key derivation are intentionally incompatible; sharing Rails secrets or accepting both formats would weaken ownership. Representation may differ, but identity, authorization, attributes, expiry, rotation, invalidation, CSRF, nonce, isolation and user-visible behaviour remain gated. Required evidence is defined in `migration/session-ownership-plan.md` and R-01/R-02. | Security and architecture approvers required; none yet | Rails characterization; returning-user/account-linking; OIDC error/state/nonce; cookie rotation/tamper/timeout; complete logout and replay; cross-session CSRF and `form_nonce`; concurrent isolation; gateway ownership; deployment key persistence; rollback | proposed |

Security defects must not be reproduced, but classification requires an explicit security review and approval recorded in this table.
