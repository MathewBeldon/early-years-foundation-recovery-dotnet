# Migration evidence

Commit only deterministic, reviewed evidence suitable for source control. Playwright traces, videos, raw screenshots and reports are generated under `tmp/parity` and uploaded by CI; they are not committed by default. Never place credentials, master keys, API keys or production data here.
